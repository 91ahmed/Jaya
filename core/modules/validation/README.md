```php
	/**
	 * check the data from the incoming http request from GET and POST request.
	 * @param string (input name)
	 */
	request('input name')

	/**
	 * check the user data.
	 * @param mixed (data)
	 * @param string *optional* (label)
	 */
	data('your data', 'label')
```